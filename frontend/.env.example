# Base URL of the backend API. No trailing slash.
#
# .env             -> local development (this value)
# .env.production  -> used automatically by `vite build` / deployments
VITE_API_URL=http://localhost:5000/api
